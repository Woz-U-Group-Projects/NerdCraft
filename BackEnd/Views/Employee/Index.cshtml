@model IEnumerable<NerdCraft.Models.employee>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Userpass)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IDNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SupervisorNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Userpass)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IDNavigation.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SupervisorNavigation.EmployeeEmail)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EmployeeEmail">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EmployeeEmail">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EmployeeEmail">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
